struct square {
  size: u32;
  pub static fn create(size: u32): square {
    return square {
      size
    };
  }
  pub fn testFn(isTest: bool) {
    const b = 2;
  }
}

fn thing(shape: square) {
  const a = 2;
}

const s = square.create(10);

thing(s);

// square.testFn(true);
