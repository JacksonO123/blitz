struct shape {
  pub area: u32;

  prot fn getArea() u32 {
    return self.area;
  }
}

struct square : shape {
  size: u32;

  pub fn getShapeArea() u32 {
    return self.getArea() + 1;
  }

  fn internalFn() {
    let b = 2;
  }
}

fn thing(sh: square) {
  let a = 2;
}

let len = 5;

let s = square {
  area = (len * len),
  size = len
};

{
  s.getShapeArea();
  
  let mut b = 2;
}

// should throw errors
// b = 3;
// s.internalFn();
