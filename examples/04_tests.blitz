fn testingExhaustiveIf(input: bool): bool {
  if (input) {
    return true;
  } else {
    return false;
  }
}

const a = testingExhaustiveIf(false);

fn testForReturn(input: u32): bool {
  return true;

  for (var i = 0; i < input; i++) {
    return true;
  }

  while (true) {
    return true;
  }

  // return true;
  
  // {
    // return false;
  // }
}

const b = testForReturn(2);
