struct Car {
  size: u32;
}

struct [T: Car] name {
  value1: bool;
  value2: T;

  pub static fn create(value1: bool, value2: T) name<T> {
    return name<T> {
      value1 = !value1,
      value2
    };
  }
}

let a = Car { size = 10 };
let n: name<Car> = name.create(true, a);
