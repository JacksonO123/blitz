struct Wrapper [T] {
    data: T;
    pub offset: u16;
}

fn thing[T](data: T) Wrapper<T> {
    return Wrapper<T> {
        data,
        offset = 3u16,
    };
}

fn main() {
    let arr = [1, 2, 3, 4];
    let a = thing(arr.len);
    let b = a.offset;
}
